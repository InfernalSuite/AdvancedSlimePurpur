
import com.infernalsuite.asp.conventions.PublishConfiguration.Companion.publishConfiguration

//Do nothing on ASPaper. We don't want to publish anything
val publishConfiguration = publishConfiguration()
